
import * as React from 'react';
import {Spinner as BpSpinner} from '@blueprintjs/core';

const Spinner : React.StatelessComponent<{}> = () =>
  <BpSpinner size={BpSpinner.SIZE_SMALL} />;

export default Spinner;
